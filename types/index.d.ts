export type FormInputPost = {
    title: string;
    content: string;
    tagId: string;
    authorId: string;
};

export type User = {
    first_name: string;
    last_name: string;
    email: string;
    profile_image: string;
};
